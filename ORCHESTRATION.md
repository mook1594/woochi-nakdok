# ORCHESTRATION.md

`nakdok`의 **진행 상태 SSOT**이자 작업 진행 규칙.

| 알고 싶은 것 | 볼 곳 |
|---|---|
| 무엇을 만드는가 | `docs/requirements.md` — EARS 요구사항 |
| 왜 그렇게 만드는가 | `docs/architecture.md` — 결정 D1~D9 |
| 어떤 순서로 | `docs/roadmap.md` — Phase 종료 조건 |
| 프로젝트 불변 규칙 | `CLAUDE.md` |
| **지금 어디까지 왔는가** | **이 문서** |
| 세션 간 인수인계 | `docs/handoff.md` — 최신 1개만 유지 |

---

## 역할 분담

**오케스트레이터(메인 세션)는 구현하지 않는다.** 작업을 쪼개고, 서브에이전트에
임무를 주고, 결과를 **독립 검증**하고, 커밋하고, 이 문서와 핸드오프를 갱신한다.

**서브에이전트가 구현한다.** 한 번에 한 작업. 임무서에 적힌 파일만 건드린다.
자기보고는 증거가 아니다 — 검증은 오케스트레이터가 직접 실행해서 한다.

---

## 상태 값

| 값 | 뜻 |
|---|---|
| `대기` | 시작 안 함 |
| `진행중` | 서브에이전트 작업 중 |
| `검증중` | 반환됨. 오케스트레이터가 검증하는 중 |
| `완료` | 검증 통과 + 커밋됨 |
| `막힘` | 에스컬레이션 대기 — 사람의 답이 있어야 진행 |

---

## 진행 현황

### 세션 0 — 규칙 정립

| ID | 작업 | 상태 | 커밋 |
|---|---|---|---|
| S0.1 | 언어·스택 확정 (Python) | `완료` | `d72092a` |
| S0.2 | 개발 환경 (`.venv` + supertonic 검증) | `완료` | `eff8eec` |
| S0.3 | 설계 결정 3개 해소 | `완료` | `749f229` `bf3d413` `ca801e9` |
| S0.4 | `ORCHESTRATION.md` · `handoff.md` · 에이전트 정의 | `완료` | 이 커밋 |

### 세션 1 — 스캐폴딩과 입력

| ID | 작업 | 요구사항 | 상태 |
|---|---|---|---|
| S1.0 | 세션 0 이월 — 종료 절차에 푸시 확인 단계 추가 | — | `완료` |
| T1 | 패키지 스캐폴딩 + CLI 4개 명령 | — | `완료` |
| T2 | 인코딩 감지 | R1.1~1.4 | `대기` |
| T3 | 챕터 분할 + 설정 로딩 | R2.1~2.5 | `대기` |

### 세션 2 — 분할과 매니페스트

| ID | 작업 | 요구사항 | 상태 |
|---|---|---|---|
| T4 | 청크 분할 + `boundary_after` | R3.1~3.8 | `대기` |
| T5 | 매니페스트 생성 | R4.1~4.5, R10.1 | `대기` |

### 세션 3 — 합성과 캐시

| ID | 작업 | 요구사항 | 상태 |
|---|---|---|---|
| T6 | TTS 합성 | R5.1~5.7 | `대기` |
| T7 | 캐시 no-op + 고아 파일 | R10.2~10.4 | `대기` |

### 세션 4 — 출력

| ID | 작업 | 요구사항 | 상태 |
|---|---|---|---|
| T8 | 무음 삽입 | R6.1~6.3 | `대기` |
| T9 | m4b 빌드 | R7.1~7.5 | `대기` |
| T10 | `run` 파이프라인 | — | `대기` |

### 사람이 해야 하는 것 (병렬)

| 작업 | 상태 | 막고 있는 것 |
|---|---|---|
| **Phase 0 청취** — 보이스 1종 + `speed` 확정, 오독 토큰 수집 | `대기` | **T6.** T1~T5는 이것 없이 진행 가능 |

---

## 환경 재현

```
python -m venv .venv
.venv/Scripts/python.exe -m pip install -e .
.venv/Scripts/python.exe -m pip install pytest
```

T1이 `pyproject.toml`을 만들기 전까지는 두 번째 줄 대신 이걸 쓴다:

```
.venv/Scripts/python.exe -m pip install supertonic soundfile pyyaml pytest
```

**가상환경은 자동 활성화되지 않는다.** 항상 `.venv/Scripts/python.exe`로 부른다.
한글을 출력하는 스크립트에는 `PYTHONIOENCODING=utf-8`을 붙인다 — 콘솔이 cp1252라
안 붙이면 크래시한다.

## 모듈 배치

작업 경계와 파일 경계를 일치시킨다. 서브에이전트에 "이 파일만"이라고 말할 수 있어야
검증이 값싸진다.

```
pyproject.toml          # 패키지 + entry point: nakdok = nakdok.cli:main
nakdok/
  __init__.py
  cli.py                # argparse, 4개 서브커맨드          (T1)
  config.py             # .nakdok/config.yaml 로딩 + 기본값   (T3)
  analyze.py            # 인코딩·챕터·문장·청크·boundary_after (T2 T3 T4)
  manifest.py           # 스키마·text_hash·읽기/쓰기          (T5)
  synth.py              # TTS 호출 + 캐시                     (T6 T7)
  build.py              # 무음 + ffmpeg concat/인코딩         (T8 T9)
tests/
  test_*.py
```

CLI는 `argparse`(표준 라이브러리)를 쓴다. `click`이 supertonic의 전이 의존성으로
깔려 있지만, 우리가 선언하지 않은 패키지에 기대면 supertonic이 그걸 버릴 때 깨진다.

---

## 작업 상세

각 항목이 곧 서브에이전트 임무서의 원본이다. 완료 기준은 **실행 가능한 명령**으로
적는다 — "동작한다"는 완료 기준이 아니다.

### T1 — 패키지 스캐폴딩 + CLI

- **건드려도 되는 파일**: `pyproject.toml`, `nakdok/__init__.py`, `nakdok/cli.py`, `tests/test_cli.py`
- **완료 기준**
  - `.venv/Scripts/python.exe -m pytest` 가 통과한다 (테스트 0개는 통과가 아니다)
  - `.venv/Scripts/nakdok.exe --help` 가 `analyze` `synth` `build` `run` 4개를 출력한다
  - 각 서브커맨드가 아직 구현되지 않았음을 알리고 0이 아닌 코드로 끝난다
- **테스트**
  - 파서가 4개 서브커맨드를 등록했는지 (이름 집합 비교)
  - `analyze`가 인자 없이 호출되면 에러로 끝나는지
- **주의**: `pyproject.toml`에 `supertonic` · `soundfile` · **`pyyaml`**을 직접
  선언한다. 셋 다 이미 `.venv`에 깔려 있지만 `pyyaml`은 supertonic이 끌고 온
  **전이 의존성**이다 — 선언하지 않으면 supertonic이 그걸 버리는 순간 설정 로딩이
  조용히 깨진다. `click`을 안 쓰기로 한 것과 같은 이유다. `pytest`는 아직 설치돼
  있지 않으므로 개발 의존성으로 선언하고 설치한다

### T2 — 인코딩 감지 (R1.1~1.4)

- **건드려도 되는 파일**: `nakdok/analyze.py`, `nakdok/cli.py`, `tests/test_analyze.py`
- **완료 기준**: R1.1~1.4가 각각 테스트로 증명된다
- **테스트**
  - UTF-8 파일 → 성공 (R1.1)
  - CP949 파일 → 성공 + 감지된 인코딩을 표준 출력에 보고 (R1.2)
  - 두 인코딩 모두 실패하는 바이트열 → exit 2 + 실패한 인코딩 목록 출력 (R1.3)
  - 0바이트 파일 → exit 2 (R1.4)
- **주의**: Python은 `cp949` 코덱이 내장이다. 별도 패키지가 필요 없다

### T3 — 챕터 분할 + 설정 로딩 (R2.1~2.5)

- **건드려도 되는 파일**: `nakdok/analyze.py`, `nakdok/config.py`, `tests/test_analyze.py`, `tests/test_config.py`
- **완료 기준**: R2.1~2.5가 각각 테스트로 증명된다
- **테스트**
  - 기본 정규식 3종(`제\s*\d+\s*[장화부]`, `^\d+\.?\s*$`, `^[Cc]hapter\s+\d+`)이 각각 매칭 (R2.2)
  - `config.yaml`에 `chapter_pattern`이 있으면 기본값 대신 사용 (R2.3)
  - `config.yaml`이 없으면 기본값 사용
  - 챕터 경계 0개 → 단일 챕터 + 경고 출력 (R2.4)
  - 챕터 수와 각 챕터 문자 수를 표준 출력에 보고 (R2.5)

### T4 — 청크 분할 + `boundary_after` (R3.1~3.8)

- **건드려도 되는 파일**: `nakdok/analyze.py`, `tests/test_analyze.py`
- **완료 기준**: R3.1~3.8이 각각 테스트로 증명된다
- **테스트**
  - 따옴표(`"`, `'`, `「」`) 내부 마침표를 문장 경계로 보지 않음 (R3.2)
  - 줄임표(`…`, `...`)를 문장 경계로 보지 않음 (R3.3)
  - 연속 문장을 120자 이하로 누적 (R3.4)
  - 120자 초과 단일 문장은 단독 청크 (R3.5)
  - 문단 경계를 넘어 병합하지 않음 (R3.6)
  - `boundary_after` 4종 판정과 우선순위 `chapter` > `scene` > `paragraph` > `sentence` (R3.7)
  - 마지막 청크는 `chapter` (R3.8)
- **먼저 할 것**: `architecture.md` D9에 따라 **테스트를 먼저 쓰고** kss를 붙여
  통과 여부를 본다. 통과하면 kss 채택, 아니면 직접 구현. 순서를 뒤집지 않는다

### T5 — 매니페스트 생성 (R4.1~4.5, R10.1)

- **건드려도 되는 파일**: `nakdok/manifest.py`, `nakdok/analyze.py`, `tests/test_manifest.py`
- **완료 기준**: R4.1~4.5, R10.1이 각각 테스트로 증명된다
- **테스트**
  - `.nakdok/manifest.json` 생성 (R4.1)
  - 청크가 10필드를 정확히 갖는다 — `id` `chapter` `order` `boundary_after` `text` `text_hash` `voice` `speed` `audio_path` `duration_ms` (R4.2)
  - `text`가 원문과 **문자 단위로 동일**하다 (R4.3) — 이게 `CLAUDE.md` 절대 규칙 1
  - 재실행 시 `text_hash` 동일 항목의 `audio_path`·`duration_ms` 보존 (R4.4)
  - `voice`/`speed`가 config에서 오고, 없으면 `M3`/`0.95` (R4.5)
  - `text_hash`가 `SHA256(치환 후 텍스트 + voice + speed)`로 결정적 (R10.1)
  - voice를 바꾸면 해시가 바뀐다

### T6 — TTS 합성 (R5.1~5.7)

- **건드려도 되는 파일**: `nakdok/synth.py`, `nakdok/cli.py`, `tests/test_synth.py`
- **완료 기준**: R5.1~5.7이 각각 테스트로 증명된다. **실제 TTS를 부르지 않는다**
- **테스트** (가짜 TTS를 주입해서)
  - `audio_path`가 비었거나 파일이 없으면 합성 대상 (R5.1, R5.2)
  - 성공 시 `.nakdok/audio/<text_hash>.wav` 저장 + `duration_ms`를 파형 길이로 계산 (R5.3)
  - 보이스 스타일·`speed`·`lang="ko"`가 전달됨 (R5.4)
  - 예외 발생 시 청크 `id`와 오류를 기록하고 **다음 청크로 진행** (R5.5)
  - 합성/캐시/실패 카운트 보고 (R5.6)
  - 실패 1개 이상 → exit 1 (R5.7)

### T7 — 캐시 no-op + 고아 파일 (R10.2~10.4)

- **건드려도 되는 파일**: `nakdok/synth.py`, `tests/test_synth.py`
- **완료 기준**
  - 변경 없이 `synth`를 두 번 돌리면 **두 번째는 합성 호출 0회** (R10.3) — 호출 카운터로 증명
  - 매니페스트가 참조하지 않는 `.nakdok/audio/` 파일 목록 보고 (R10.4)

### T8 — 무음 삽입 (R6.1~6.3)

- **건드려도 되는 파일**: `nakdok/build.py`, `nakdok/config.py`, `tests/test_build.py`
- **완료 기준**: R6.1~6.3이 각각 테스트로 증명된다
- **테스트**
  - 인접 두 청크 사이에 앞 청크의 `boundary_after`에 맞는 무음 (R6.1)
  - 기본값 `sentence` 400 / `paragraph` 800 / `scene` 1800 / `chapter` 1000 (R6.2)
  - `config.yaml` 지정 시 기본값 대체 (R6.3)
  - 마지막 청크 뒤에는 무음이 붙지 않는다

### T9 — m4b 빌드 (R7.1~7.5)

- **건드려도 되는 파일**: `nakdok/build.py`, `tests/test_build.py`
- **완료 기준**: R7.1~7.5가 각각 테스트로 증명된다. **실제 ffmpeg/ffprobe를 쓴다**
- **테스트**
  - `chapter`·`order` 순서대로 결합 (R7.1)
  - AAC 48kbps 모노, `<입력파일명>.m4b` (R7.2)
  - **ffprobe로 잰 총 길이 == `duration_ms` 합 + 무음 합, 오차 1초 이내** — 로드맵 Phase 1 종료 조건
  - 챕터 시작 시각이 챕터 메타데이터로 들어감 (R7.3)
  - 제목 태그 (R7.4)
  - `audio_path`가 빈 청크가 있으면 exit 1 + 목록 출력 + **m4b 미생성** (R7.5)
- **주의**: 짧은 합성 wav(무음 또는 톤) 2~3개로 픽스처를 만든다. 실제 낭독이 필요 없다

### T10 — `run` 파이프라인

- **건드려도 되는 파일**: `nakdok/cli.py`, `tests/test_cli.py`
- **완료 기준**: `run`이 `analyze` → `synth` → `build`를 순서대로 실행한다
- **테스트**
  - 가짜 TTS로 txt 입력 → m4b 출력까지 end-to-end 1회
  - 중간 단계가 0이 아닌 코드로 끝나면 뒤 단계를 실행하지 않는다

---

## 에스컬레이션 규칙

**다음 셋에서만 멈추고 사람에게 묻는다.**

| | 조건 |
|---|---|
| (a) | 문서에 아직 해소되지 않은 설계 결정이 있을 때 |
| (b) | 변경이 인가·보안 표면을 건드릴 때 |
| (c) | **같은 작업에서 검증이 두 번 실패했을 때** |

그 외에는 확인 없이 진행하고, 사람이 알아야 할 것만 `docs/handoff.md`에 리포트로
남긴다.

추가로 `CLAUDE.md`가 정한 것: **요구사항에 없는 기능은 임의로 추가하지 않는다.**
필요하다고 판단되면 `requirements.md`에 항목을 제안하고 승인을 받은 뒤 구현한다.
서브에이전트가 요구사항 밖의 코드를 들고 오면 그 자체가 검증 실패다.

---

## 검증 프로토콜

서브에이전트가 결과를 반환하면 오케스트레이터가 **직접 실행해서** 확인한다.
자기보고("테스트를 통과했습니다")는 증거로 치지 않는다.

1. `.venv/Scripts/python.exe -m pytest` — 전체 실행. 해당 작업의 테스트만이 아니다
2. `git diff --stat` — 임무서에 없는 파일이 바뀌었는지
3. 완료 기준 항목별 대조 — 요구사항 번호마다 대응 테스트가 실재하는지
4. **뮤테이션 확인** — 새 로직 한 곳을 일부러 깨뜨리고 `pytest`가 **실패하는지**
   본다. 실패하지 않으면 그 테스트는 아무것도 증명하지 않는다. 확인 후 되돌린다

검증이 실패하면 **실패 출력 전문과 함께** 같은 서브에이전트에 재분배한다.
두 번째도 실패하면 에스컬레이션 (c)에 걸린다 — 멈추고 사람에게 보고한다.

검증을 통과한 작업마다 **따로** conventional commit으로 커밋하고, 이 문서의 상태와
`docs/handoff.md`를 갱신한다. 커밋 메시지에 요구사항 ID를 남긴다
(예: `feat(analyze): 챕터 분할 (R2.1~R2.5)`).

---

## 세션 시작 절차

### 1. 전제 감사

이번 세션의 작업이 딛고 있는 **사실 주장**을 먼저 목록화한다. 다음 형태의 문장이
전부 해당한다:

- "이 라이브러리가 X를 지원한다"
- "다른 프로젝트에 선례가 있어 재사용된다"
- "여기가 성능 병목이다"
- "이 방법 말고는 없다"

각각에 검증 여부를 표시하고, **미검증 항목은 계획을 확정하기 전에** 값싼 방법으로
확인한다 — 패키지를 열어보기, 해당 리포 상태 확인, 상수 grep.

채택 근거로 나열된 항목일수록 검증이 느슨하다. 채택을 정당화하려고 쓰인 문장이기
때문이다. 문서에 적혀 있다는 것은 누군가 한 번 그렇게 믿었다는 뜻이지 사실이라는
뜻이 아니다.

> **왜 이 절차가 있는가** (2026-08-28, 세션 0)
> `architecture.md`가 스택 채택 근거로 "C#/.NET SDK 공식 지원"과 "woochi-steno의
> whisper-server 패턴 재사용"을 들었고, 이를 검증 없이 받아들여 .NET 기준 작업
> 목록을 전부 세웠다. **둘 다 사실이 아니었다.** 계획을 통째로 다시 썼다.
> 확인하는 데 든 시간은 휠 하나 여는 2분이었다.
>
> 계획이 틀리면 고치면 되지만, 토대가 틀리면 계획 전체를 버린다.

### 2. 상태 복원

`docs/handoff.md`를 읽고 이전 세션이 어디서 멈췄는지, 무엇이 막혀 있는지 확인한다.

### 3. 다음 작업 확인

이 문서의 진행 현황에서 다음 `대기` 작업을 찾고, 선행 조건(사람 대기 항목 포함)이
충족됐는지 본다.

---

## 세션 종료 절차

1. 이 문서의 상태 컬럼을 실제와 맞춘다
2. `docs/handoff.md`를 이번 세션 기준으로 덮어쓴다 (히스토리는 git이 갖는다)
3. **암묵적 지식 점검** — 이 세션에서 결정했지만 문서에 안 적힌 것이 있는지 훑고,
   기입이 필요한 것은 **사람 확인 후** 반영한다
4. **PR 제목·본문을 넘기기 전에 모든 커밋이 푸시됐는지 확인한다** — `git status -sb`가
   `ahead`를 보이지 않아야 한다. PR을 넘긴 뒤에는 그 세션에 커밋을 추가하지 않는다.
   추가가 필요해지면 **다음 세션의 첫 작업**으로 넘긴다. **PR 생성은 사람이 직접 한다**

   > 세션 0에서 두 번 어겼다. PR 본문을 넘긴 뒤 커밋을 더 올려서, 머지에 누락되고
   > PR을 하나 더 만들어야 했다. 순서가 문서에 있어도 확인 단계가 없으면 지켜지지
   > 않는다.
5. 오케스트레이션 방식 자체에 개선점이 있으면 사람과 협의 후 이 문서나 에이전트
   정의를 고친다
